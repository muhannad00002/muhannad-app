# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Secrets & local env — never commit
.env
.env.*
!.env.example

# Runtime data (keep the directories, ignore the contents)
data/*.db
data/*.db-*
data/output/*
!data/output/.gitkeep
logs/*
!logs/.gitkeep
