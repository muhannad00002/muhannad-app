{
  "name": "wedding-and-co",
  "version": "2.0.0",
  "private": true,
  "description": "Wedding & Co — wedding planner & marketplace (app + serverless API).",
  "scripts": {
    "build": "node app/build.js",
    "start": "node server/server.js",
    "test": "node server/test.js && node server/db.test.js",
    "native:install": "npm i @capacitor/core @capacitor/cli @capacitor/ios @capacitor/android @capacitor/splash-screen @capacitor/assets",
    "native:add": "npx cap add ios && npx cap add android",
    "native:assets": "npx capacitor-assets generate --iconBackgroundColor #2B2B2F --splashBackgroundColor #2B2B2F",
    "cap:sync": "node app/build.js && npx cap sync",
    "cap:ios": "node app/build.js && npx cap sync ios && npx cap open ios",
    "cap:android": "node app/build.js && npx cap sync android && npx cap open android"
  },
  "engines": { "node": "22.x" },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
